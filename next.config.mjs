/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'be-express-rose.vercel.app'
        ]
    },
};

export default nextConfig;
